[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Build, package and release automation. Runs on the development host only."

[dependencies]
flux-core = { workspace = true }
# Already in the workspace via flux-core; used to read Cargo.toml, Cargo.lock
# and engine.lock. Not a new dependency (governance §1.2).
toml = { workspace = true }
